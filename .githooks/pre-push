#!/bin/sh

while read -r local_ref local_oid remote_ref remote_oid
do
  case "$remote_ref" in
    refs/heads/test-branch)
      ;;
    *)
      echo "Push rejected: this repository only permits pushes to test-branch." >&2
      exit 1
      ;;
  esac
done

exit 0