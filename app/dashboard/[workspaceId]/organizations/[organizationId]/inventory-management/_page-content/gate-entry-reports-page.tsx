"use client";

import { useParams } from "next/navigation";
import { useCallback, useEffect, useMemo, useState } from "react";
import { Download, FileBarChart, RefreshCw } from "lucide-react";

type Entry = { id: string; entry_no: string; direction: string; movement_type: string; challan_no: string | null; person_name: string; company_name: string | null; vehicle_number: string | null; entry_at: string; status: string };

export default function GateEntryReportsPage() {
  const params = useParams<{ organizationId: string }>();
  const organizationId = params?.organizationId ?? "";
  const [entries, setEntries] = useState<Entry[]>([]);
  const [direction, setDirection] = useState("ALL");
  const [movementType, setMovementType] = useState("ALL");
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState("");
  const load = useCallback(() => fetch(`/api/inventory/gate-entries?organizationId=${encodeURIComponent(organizationId)}`, { cache: "no-store" }).then(async (response) => { const data = await response.json(); if (!response.ok) throw new Error(data?.error || "Unable to load reports."); setEntries(data.entries ?? []); }).catch((loadError) => setError(loadError instanceof Error ? loadError.message : "Unable to load reports.")).finally(() => setLoading(false)), [organizationId]);
  useEffect(() => { if (organizationId) void load(); }, [organizationId, load]);
  const filteredEntries = useMemo(() => entries.filter((entry) => (direction === "ALL" || entry.direction === direction) && (movementType === "ALL" || entry.movement_type === movementType)), [entries, direction, movementType]);
  const exportCsv = () => { const rows = [["Entry No", "Direction", "Type", "Person / Party", "Company", "Challan", "Vehicle", "Date", "Status"], ...filteredEntries.map((entry) => [entry.entry_no, entry.direction, entry.movement_type, entry.person_name, entry.company_name || "", entry.challan_no || "", entry.vehicle_number || "", new Date(entry.entry_at).toLocaleString("en-IN"), entry.status])]; const blob = new Blob([rows.map((row) => row.map((cell) => `"${String(cell).replaceAll('"', '""')}"`).join(",")).join("\n")], { type: "text/csv;charset=utf-8" }); const url = URL.createObjectURL(blob); const link = document.createElement("a"); link.href = url; link.download = "gate-entry-report.csv"; link.click(); URL.revokeObjectURL(url); };
  return <main className="mx-auto max-w-[1500px] space-y-6"><header className="flex flex-col gap-4 border-b border-slate-200 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><p className="erp-eyebrow">Inventory / Gate Control</p><h1 className="erp-page-heading mt-1">Gate Entry Reports</h1><p className="mt-2 text-sm text-slate-500">Review, filter, and export the centralized inward and outward movement register.</p></div><div className="flex gap-2"><button type="button" onClick={() => void load()} className="inline-flex items-center gap-2 rounded-lg border border-slate-300 bg-white px-4 py-2.5 text-xs font-bold text-slate-700"><RefreshCw size={15} /> Refresh</button><button type="button" onClick={exportCsv} className="inline-flex items-center gap-2 rounded-lg bg-emerald-700 px-4 py-2.5 text-xs font-bold text-white"><Download size={15} /> Export CSV</button></div></header>{error ? <div className="rounded-lg border border-red-200 bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div> : null}<section className="grid gap-4 sm:grid-cols-3"><div className="erp-surface p-4"><p className="text-xs font-bold uppercase tracking-widest text-slate-500">Total records</p><p className="mt-2 text-3xl font-bold text-slate-950">{filteredEntries.length}</p></div><div className="erp-surface p-4"><p className="text-xs font-bold uppercase tracking-widest text-emerald-700">Inward</p><p className="mt-2 text-3xl font-bold text-emerald-700">{filteredEntries.filter((entry) => entry.direction === "INWARD").length}</p></div><div className="erp-surface p-4"><p className="text-xs font-bold uppercase tracking-widest text-amber-700">Outward</p><p className="mt-2 text-3xl font-bold text-amber-700">{filteredEntries.filter((entry) => entry.direction === "OUTWARD").length}</p></div></section><section className="erp-surface flex flex-col gap-4 p-5 sm:flex-row sm:items-end"><label className="block w-full text-xs font-bold text-slate-700 sm:max-w-xs">Direction<select value={direction} onChange={(event) => setDirection(event.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 text-sm"><option value="ALL">All directions</option><option value="INWARD">Inward</option><option value="OUTWARD">Outward</option></select></label><label className="block w-full text-xs font-bold text-slate-700 sm:max-w-xs">Movement type<select value={movementType} onChange={(event) => setMovementType(event.target.value)} className="mt-1.5 w-full rounded-lg border border-slate-300 px-3 py-2.5 text-sm"><option value="ALL">All types</option><option value="CHALLAN">Material Challan</option><option value="VISITOR">Visitor</option><option value="STAFF">Staff Movement</option><option value="COURIER">Courier</option><option value="OTHER">Other</option></select></label><div className="flex items-center gap-2 text-xs text-slate-500"><FileBarChart size={18} /> Showing latest {filteredEntries.length} matching records</div></section><section className="erp-surface overflow-hidden"><div className="overflow-x-auto">{loading ? <p className="p-8 text-center text-xs text-slate-500">Loading reports...</p> : <table className="w-full min-w-[1050px] text-left text-xs"><thead className="border-b border-slate-200 bg-slate-50 text-[9px] font-bold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Entry no.</th><th className="px-4 py-3">Direction</th><th className="px-4 py-3">Type</th><th className="px-4 py-3">Person / party</th><th className="px-4 py-3">Company</th><th className="px-4 py-3">Challan</th><th className="px-4 py-3">Vehicle</th><th className="px-4 py-3">Date and time</th><th className="px-4 py-3">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{filteredEntries.map((entry) => <tr key={entry.id}><td className="px-4 py-3 font-bold text-slate-900">{entry.entry_no}</td><td className={`px-4 py-3 font-bold ${entry.direction === "INWARD" ? "text-emerald-700" : "text-amber-700"}`}>{entry.direction}</td><td className="px-4 py-3">{entry.movement_type}</td><td className="px-4 py-3">{entry.person_name}</td><td className="px-4 py-3">{entry.company_name || "-"}</td><td className="px-4 py-3">{entry.challan_no || "-"}</td><td className="px-4 py-3">{entry.vehicle_number || "-"}</td><td className="px-4 py-3">{new Date(entry.entry_at).toLocaleString("en-IN")}</td><td className="px-4 py-3">{entry.status}</td></tr>)}</tbody></table>}{!loading && filteredEntries.length === 0 ? <p className="p-8 text-center text-xs text-slate-500">No matching gate entries found.</p> : null}</div></section></main>;
}
